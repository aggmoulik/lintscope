{
  "name": "lintscope-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Sample project with intentional lint errors. Used by `pnpm demo` to populate the dashboard.",
  "type": "module",
  "devDependencies": {
    "eslint": "^10.4.0"
  }
}
